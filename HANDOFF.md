# Handoff

**2026-08-18 — series status computed at build; Docker gets its own cover.**

## What shipped this session

| Work | Notes |
|------|--------|
| Public series table | `{{< series-schedule >}}` on the overview post. Live vs Scheduled vs Held from `date` + `draft` vs now. Daily Pages rebuild flips Friday’s landing-floor row with no commit. Future parts are listed, not linked (no 404). |
| Data | `data/series/agent-production-system.yaml` generated by `scripts/sync-series-schedule.py`. `make check` / `make series-schedule`. |
| Docs table | Status column removed. Dates + slugs only. |
| Unique covers | Docker cover is `docker-homelab-hosts.jpg`. Scaffold no longer defaults to the overview hero. Check fails if two series posts share a `[cover]`. |

## Local-only (not committed)

- `.gitignore` extra landing-floor ignore lines. Leave them.
- Desktop: `~/Desktop/docker-cover-picks/` (unused household alt). Safe to delete.

## Where to start next session

1. After Pages: hard-refresh `/posts/agent-production-system/` — parts 0–7 Live, 8 Scheduled (until 2026-08-21).
2. New series part: unique cover + `make series-schedule`. Do not add a Status cell.

## Do not

- Do not hand-edit Live/Scheduled into the overview table or the docs schedule.
- Do not reuse another part’s cover.
- Do not change the landing-floor date unless Dave asks.
