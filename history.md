# History

- 2026-07-21: Checked GitHub boards (only #24/#55 open, no project-board items), wrote and shipped a "We're Back" comeback post via `/grilling` (PR #79), and fixed the reported `/graph/` confusion by adding an explanatory caption rather than rebuilding anything — `docs/research/graph-view-ux-patterns.md`'s R1/R2 edge-density fixes were already shipped, the page just had zero text explaining what a dot/line/isolated-dot meant (PR #80). Also ran `scripts/enable-landing-floor.sh` against this repo for the first time (Dave's explicit ask), which surfaced that `git-safety.sh` now blocks raw `gh pr merge` here — both PRs landed instead through the full review-lenses receipt → `land-pr.sh` sanctioned path.
- 2026-07-19: Orchestrated plan 0004's full 8-deliverable frontier (#59-66, PRs #69-#77) solo, single-orchestrator. D1-D3 built a persistent bio sidebar + sticky scroll-spy TOC across every page; D4-D8 added a new wikilinked `/vault/` section (Goldmark render hook, backlink index, graph integration, 4 real seed notes). review-lenses caught and fixed real bugs on nearly every PR — a layout regression that would've affected all 76 posts (D1/D2), a broken taxonomy lookup site-wide (D4), a title-collision silent-wrong-link bug and a `www.`-link regression (D5), duplicate backlink rows (D6), and a double-edge/dangling-edge risk in the graph (D7). One deliverable (D5/PR #73) was merged manually by Dave mid-session before a fix commit could ride along, cleanly re-landed as PR #74 (cherry-pick) with zero content loss. `feat/wikilink-render-hook` left unpruned (documented in HANDOFF.md) — its content is safely in `main` but its exact tip SHA can't be proven an ancestor, so the verifying prune script correctly refused to auto-delete it.
- 2026-07-19/20: Researched ssp.sh (Simon Späti's Hugo blog + second brain) at Dave's request, ran a 7-round grilling session, and shipped plan 0004 (persistent bio sidebar, sticky scroll-spy TOC, and a new wikilinked "Vault" content section extending the existing graph view) as a Lavish-approved plan doc + 4 ADRs (PR #58) + 8 dependency-wired issues (#59-66) exported to the Agent Work board (PR #67). Stop-at-doc hand-off per ADR 0003 — no implementation yet; a fresh orchestrator session claims the frontier next.
- 2026-07-18: Repo created; plan 0001 (rebuild davevoyles.com as a Hugo static site on GitHub Pages, content recovered from Wayback Machine) grilled, approved via Lavish, and exported to issues #1-#9 on the Agent Work board. No implementation started yet.
- 2026-07-18: Unflagged 33 of D6's 71 stale-flagged posts per Dave's call ("keep all of these unless the page doesn't render") — those 33 had no broken image references, only dead-tech keywords/age/dead text links, none of which affect rendering. Verified via a real Hugo build+serve (initializing the PaperMod theme submodule, previously skipped) and confirmed the remaining 38 flagged posts reference images on `davevoyles.azurewebsites.net`, a genuinely dead domain (real DNS NXDOMAIN) — left those flagged pending Dave's decision on how to handle the images. Also caught and fixed an accidental `public/`/`.hugo_build.lock` commit from the verification build, adding both to `.gitignore`.
- 2026-07-18: Stripped dead images and unflagged the remaining 38 D6-flagged posts per Dave's call ("unflag but strip the dead img tags first"). New `scripts/strip_dead_images_and_unflag.py` live-checks and removes only broken image markdown (reusing `check_url` from D6's triage script), confirmed by re-scanning for leftovers before unflagging. 92 broken image constructs removed across 6 dead hosts; all 76 posts on the site are now `draft = false`. Deploy watched green (smoke test included); live-verified the previously-broken `console-wars-of-2013-who-will-win-big` post now renders 200 with zero `<img>` tags (after a transient 503 CDN blip right after deploy cleared on retry).
- 2026-07-18: Added a repo-map table to README.md (PR #21) so agents and new contributors can orient across `content/`, `static/`, `scripts/`, `docs/*`, and config without exploring the tree cold — part of a broader pass answering Dave's questions on post-authoring workflow, tagging/graph linking, and visual design (no code changes yet, recommendations delivered in-chat pending his picks).
- 2026-07-18: Wired up PaperMod's built-in Tags/Archives/Search nav (PR #23) and backfilled tags on 7 posts that had none — review lenses caught and fixed a real bug where the backfill copied compound category strings (`"Javascript / HTML5"`) verbatim instead of the site's existing atomic tag vocabulary, which would have fragmented the `/tags/` taxonomy; also hardened the deploy smoke test (previously root-only) to cover the 3 new routes, fixing a `set -e` abort risk on connection-level curl failures along the way. Filed the deferred graph-view idea as issue #24. Added a chat-driven post-authoring workflow to `docs/authoring-guide.md` (PR #25). Shipped a "Console" visual theme (PR #26) — 3 real-content mockups presented, Dave picked Console; testing lens caught that the initial CSS override missed the brand-new Archives/Search pages entirely (different class names / client-rendered markup), fixed and re-verified. All contrast pairs WCAG-AA checked, not eyeballed.
- 2026-07-18: Grilled and shipped plan 0002 (related-posts widget + curated `topics` taxonomy) — 6 deliverables (all XS/S), ADR 0004, `CONTEXT.md` scaffolded, issues #29-#34 created/dependency-wired/seeded to the Agent Work board (PRs #28, #35). A pre-persist `git fetch`/pull caught that the local checkout was 7 commits behind — PR #23/#26 had already shipped tag cleanup, nav, and a Console theme, and issue #24 (an Obsidian-style graph view) already existed as the actual "connect blog posts" ask from an earlier same-day conversation; surfaced to Dave mid-session rather than persisting on stale premises. Dave confirmed the `topics` field stays separate from the now-cleaner `tags`, and asked to grill #24 next.
- 2026-07-18: Orchestrated plan 0002's full execution frontier (#29-#34, PRs #37-#42) solo, single-orchestrator, no races. D1's topics-mapping script mapped 75/76 posts cleanly (1 flagged, hand-reviewed, deliberately left unassigned — a modern placeholder post fitting no curated bucket); D2 applied it for real. D3 wired Hugo's Related Content to the `topics` index; D4 added a Console-styled Related Posts widget (WCAG contrast-checked both themes). D5 (topic archive pages) hit a real bug along the way — Hugo treats a literal `/` in a taxonomy term as a URL hierarchy separator, breaking 4 of the 6 curated bucket names (`Career / Students` → nested `/topics/career-/-students/` instead of one clean page) — caught via the real build, fixed by renaming all 4 to slash-free forms across the lookup table, tests, and all 34 already-applied posts. D6 extended the deploy smoke test to cover `/topics/gaming/`; the triggered CI run was watched green post-merge. Self-review used a hand-assembled review-lenses pass throughout (the skill's own scripts aren't symlinked on this machine) — receipts posted for every PR via `review-lens-receipt.sh`.
- 2026-07-19: Fixed the shipped `/graph/` view's usability problem after Dave called it "not practical" — background research (Obsidian/Roam/Logseq/digital-garden precedent) quantified why: 42.5% edge density, hub posts touching 62-76% of the graph, driven by generic legacy tags ("Game Dev" 46% of posts, "Programming" 38%). Applied the two highest-impact fixes (exclude the meaningless "Uncategorized" tag, down-weight generic hub tags, require 2+ shared tags for an edge) — edges dropped 1,212 → 319, max degree 57 → 32, verified via real build plus hand-picked instance-level spot checks. Also added a homepage bio + LinkedIn/GitHub sidebar (PaperMod's built-in home-info block, homepage-only) and year-header grouping to the homepage post list. Along the way, "no organization to blog posts" turned out not to be a sort-order bug (already newest-first, verified live) but a real content gap: nothing exists between 2015-2024 because plan 0001/D5's WordPress converter only ever handled one of two legacy themes — filed issue #55 to track it (recovery itself unscoped). review-lenses caught a real page-boundary duplicate-year-header bug and a missed near-duplicate tag spelling ("Game Development"), both fixed before PR #56 opened. Held off auto-merge per the externally-facing-content exception (bio copy needs Dave's eyes even though he confirmed wording in-chat).
- 2026-07-18/19: Grilled, planned, and orchestrated plan 0003 (post connections graph view, issue #24 — the graph-view idea plan 0002 had deliberately deferred). Grilling confirmed 9 decisions in one session (edges from tags/categories not the coarse `topics` taxonomy, a scoped force-graph JS library — the site's first client-side JS dependency, ADR 0005 — a standalone `/graph/` nav page, Hugo-native JSON data generation, topic-colored nodes with filter toggles, click-to-navigate/hover-tooltip, touch-enabled everywhere). Lavish-reviewed (D2 was M-sized, so the all-XS/S waiver didn't apply) and approved; exported to issues #45-#48 (PRs #44/#49). Orchestrated the full frontier with delegated Haiku sub-agents per Dave's explicit instruction, independently re-verifying every delivery before merge: caught and fixed a self-referential-node bug in D2 (the new `/graph/` page was leaking into its own graph data), and — the big one — a **critical bug in D2/#51 (already merged) where force-graph's real API requires `{nodes, links}`, not `{nodes, edges}`**, silently swallowed by a try/catch and leaving every visitor to `/graph/` with a blank graph. Found during D3's self-review, confirmed by grepping the real pinned bundle (zero `.edges` references) and reproducing the crash headlessly (jsdom + canvas driving the actual library) before fixing and re-confirming. D4 extended the deploy smoke test to `/graph/`; CI watched green. All 4 deliverables' PRs carry hand-assembled review-lens receipts.
- 2026-07-21: Fixed Dave's report that `/search/` was unclickable and wouldn't accept text — root-caused via live browser reproduction to PaperMod's vendored `fastsearch.js` only clearing the search `<input>`'s `disabled` attribute inside a `window` `load` listener, which waits for every subresource on the page (not just the DOM), silently dropping keystrokes typed before it fires. Since the script already runs with `defer` (DOM guaranteed parsed), fixed by overriding the theme's file at the site level (`assets/js/fastsearch.js` shadows the pinned `themes/PaperMod` submodule via Hugo's asset lookup, rather than editing vendored/submodule code directly) and calling init immediately. Re-ran `enable-landing-floor.sh` in this fresh worktree (the symlinks are gitignored/per-worktree per the prior session's HANDOFF note) and landed through the full pipeline: router → two Explore-agent lenses (code-quality, testing) + generalist `code-review`, synthesized to "Needs Attention" (both lenses independently flagged the same real gap — no provenance marker tying the fork back to its upstream PaperMod commit, so drift would be silent), fixed before merge, receipt posted, `land-pr.sh` merged PR #83, CI watched green. Confirmed live on davevoyles.com post-deploy. Note for future sessions: this sandbox's Chrome automation cannot reach `127.0.0.1`/`localhost` (returns `chrome-error://chromewebdata/`) — only public internet origins are reachable — so local-build browser verification isn't possible here; verify fixes against the deployed site or via built-bundle inspection instead.
- 2026-07-21: Removed "Principal" from the sidebar bio (Dave's title changed) and wired up the sidebar avatar photo Dave asked to add, both via PR #84. `layouts/partials/sidebar.html` (plan 0004) already had optional `homeInfoParams.Avatar` support built in and simply unconfigured. Getting the actual photo in was the real obstacle: this sandbox can't reach either of Dave's Macs' local filesystems (paths that look valid resolve to empty/different directories here) or his OneDrive share link (`curl` hit Microsoft's WAF "request is blocked"; `WebFetch` chased the SPA redirect chain to a JS-rendered dead end; loading it in the connected Chrome browser worked and rendered the photo, but its `blob:` URL couldn't be exfiltrated as base64 — the extension's JS-eval tool deliberately blocks that). The path that worked: Dave uploaded the file directly to the repo via the GitHub web UI, then `git fetch`/pull. Replaced his 3.5MB 4032x3024 original in place with a center-cropped 480x480 (82KB) version sized for the sidebar's avatar slot. review-lenses (code-quality + testing) caught a real accessibility gap on the first pass — alt text hardcoded to the site author's name despite the photo showing two people — fixed with an optional `homeInfoParams.AvatarAlt` (same optional-with-fallback pattern as Avatar) before the second, clean pass landed via `land-pr.sh`. CI watched green, confirmed live.
