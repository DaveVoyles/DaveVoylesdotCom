# Pinned to what D1 verified working on 2026-07-27 (Python 3.13.14, macOS arm64).
# Install with: uv pip install --python .venv/bin/python -r requirements.txt
pillow==12.3.0
kokoro==0.9.4
soundfile==0.14.0

# Transitive, pinned because the espeak/phonemizer chain is the fragile part
# (see PROTOTYPE.md "Known gotcha: espeak-ng data path").
torch==2.13.0
misaki==0.9.4
phonemizer-fork==3.3.2
espeakng-loader==0.2.4
numpy==2.5.1

# YouTube Data API OAuth + upload (plan 0006 D6/D7).
google-api-python-client==2.198.0
google-auth-httplib2==0.4.0
google-auth-oauthlib==1.4.0
requests==2.34.2
