+++
title = "How I run an agent production system (video preview)"
date = "2026-07-27T12:00:00-04:00"
draft = true
author = "Dave Voyles"
description = "Local-only prototype preview: the ~60s generated explainer video embedded above the post body. Never published — see plan 0005."
categories = ["Programming", "AI"]
tags = ["AI agents", "orchestration"]
topics = ["Tech"]
+++

**This is a throwaway preview page, not a published post.** It exists only to view the
plan 0005 generated video in a real Hugo render. `draft = true` keeps it out of every
production build; the site publishes with `draft = false` + a daily rebuild, so this page
cannot reach davevoyles.com. The video itself is gitignored — regenerate it with
`video-prototype/build_video.sh all`, then run `video-prototype/preview.sh`.

{{< prototypevideo src="/video/agent-production-system-60s.mp4" poster="/video/agent-production-system-60s-poster.jpg" >}}

The real post is [How I run an agent production system](/posts/agent-production-system/).
The video is a ~60-second compression of it: eight scenes, Kokoro narration, Ken Burns
motion over the post's two illustrations plus generated text cards.
